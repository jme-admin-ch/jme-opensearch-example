<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.admin.bit.jeap</groupId>
        <artifactId>jeap-spring-boot-parent</artifactId>
        <version>38.6.1</version>
        <relativePath/>
    </parent>

    <groupId>ch.admin.bit.jeap</groupId>
    <artifactId>jme-opensearch-example</artifactId>
    <version>1.0.1-SNAPSHOT</version>
    <name>JME OpenSearch Example</name>
    <description>Example demonstrating event-driven indexing and authorization-aware searches with the jEAP OpenSearch components.</description>
    <packaging>pom</packaging>

    <url>https://github.com/jme-admin-ch/jme-opensearch-example/tree/main</url>
    <scm>
        <url>https://github.com/jme-admin-ch/jme-opensearch-example.git</url>
        <connection>scm:git:git://github.com/jme-admin-ch/jme-opensearch-example.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/jme-admin-ch/jme-opensearch-example.git</developerConnection>
    </scm>
    <developers>
        <developer>
            <name>BIT</name>
            <email>jeap-community@bit.admin.ch</email>
            <organization>Federal Office of Information Technology, Systems and Telecommunication FOITT</organization>
            <organizationUrl>https://www.bit.admin.ch/</organizationUrl>
        </developer>
    </developers>
    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <properties>
        <jeap-oauth-mock-server.version>7.2.0</jeap-oauth-mock-server.version>
        <jeap-opensearch-index-writer-service.version>3.7.0</jeap-opensearch-index-writer-service.version>
        <jme-spring-boot-integration-test.version>5.5.0</jme-spring-boot-integration-test.version>
        <java.version>25</java.version>
        <maven.compiler.release>25</maven.compiler.release>
        <maven.javadoc.skip>true</maven.javadoc.skip>
    </properties>

    <modules>
        <module>jme-opensearch-index-writer-service</module>
        <module>jme-opensearch-inspection-service</module>
        <module>jme-opensearch-resource-service</module>
        <module>jme-opensearch-auth-scs</module>
        <module>jme-opensearch-test</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
              <groupId>ch.admin.bit.jeap</groupId>
              <artifactId>jeap-opensearch-index-writer-service-instance</artifactId>
              <version>${jeap-opensearch-index-writer-service.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>ch.admin.bit.jeap</groupId>
                <artifactId>jme-opensearch-index-writer-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.admin.bit.jeap</groupId>
                <artifactId>jme-opensearch-inspection-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.admin.bit.jeap</groupId>
                <artifactId>jme-opensearch-resource-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.admin.bit.jeap</groupId>
                <artifactId>jme-opensearch-auth-scs</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <configuration>
                        <archive>
                            <manifestEntries>
                                <Bundle-License>https://www.apache.org/licenses/LICENSE-2.0</Bundle-License>
                            </manifestEntries>
                        </archive>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.honton.chas</groupId>
                <artifactId>license-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <inherited>false</inherited>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-project-license</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}/META-INF</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${maven.multiModuleProjectDirectory}</directory>
                                    <includes>
                                        <include>LICENSE</include>
                                    </includes>
                                    <filtering>false</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Attach an empty javadoc jar to every module while standard javadoc generation is skipped. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>empty-javadoc-jar</id>
                        <phase>package</phase>
                        <goals><goal>jar</goal></goals>
                        <configuration>
                            <classifier>javadoc</classifier>
                            <classesDirectory>${project.basedir}</classesDirectory>
                            <excludes><exclude>**/**</exclude></excludes>
                            <forceCreation>true</forceCreation>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
